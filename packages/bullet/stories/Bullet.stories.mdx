import { Canvas, Meta, Story, Description, ArgsTable } from "@storybook/addon-docs";
import { MarginArgTypes } from "@apptane/react-ui-storybook-docs";
import { Bullet, Pane } from "@apptane/react-ui";

<Meta
  title="Components/Primitives/Bullet"
  component={Bullet.type}
  argTypes={{
    colorMode: {
      table: {
        category: "Appearance",
      },
    },
    appearance: {
      table: {
        disable: true,
      },
    },
    intent: {
      table: {
        category: "Appearance",
      },
    },
    color: {
      table: {
        category: "Appearance",
      },
      control: "color",
    },
    size: {
      table: {
        category: "Dimensions",
      },
      control: {
        type: "range",
        min: 4,
        max: 32,
        step: 1,
      },
    },
    ...MarginArgTypes,
  }}
/>

export const Template = (args) => <Bullet {...args} />;

# Bullet

<Description of={Bullet.type} />

<Canvas withSource="none">
  <Story name="Basic">{Template.bind({})}</Story>
</Canvas>

Appearance based on intent — `none` `success` `warning` `danger` `neutral`.

<Canvas withSource="none">
  <Story name="Primary">{Template.bind({})}</Story>
  <Story name="Success" args={{ intent: "success" }}>
    {Template.bind({})}
  </Story>
  <Story name="Warning" args={{ intent: "warning" }}>
    {Template.bind({})}
  </Story>
  <Story name="Danger" args={{ intent: "danger" }}>
    {Template.bind({})}
  </Story>
  <Story name="Neutral" args={{ intent: "neutral" }}>
    {Template.bind({})}
  </Story>
</Canvas>

As a decorator for other elements.

<Canvas>
  <Story name="Decorator">
    <Bullet intent="danger">
      <Pane width={20} height={20} background="contrast" cornerRadius={4} />
    </Bullet>
  </Story>
</Canvas>

## Properties

<ArgsTable story="Basic" />
