import { Canvas, Meta, Story, Description, ArgsTable } from "@storybook/addon-docs";
import { MarginArgTypes, SelectorGroupArgTypes, StateManager } from "@apptane/react-ui-storybook-docs";
import { Badge, IconButton, Pane, Tabs } from "@apptane/react-ui";

<Meta
  title="Components/Controls/Tabs"
  component={Tabs}
  argTypes={{
    colorMode: {
      table: {
        category: "Appearance",
      },
    },
    appearance: {
      table: {
        disable: true,
      },
    },
    size: {
      table: {
        category: "Dimensions",
      },
    },
    width: {
      table: {
        category: "Dimensions",
      },
    },
    offset: {
      table: {
        category: "Layout",
      },
    },
    spacing: {
      table: {
        category: "Layout",
      },
    },
    alignment: {
      table: {
        category: "Layout",
      },
    },
    border: {
      table: {
        category: "Appearance",
      },
    },
    ...SelectorGroupArgTypes,
    ...MarginArgTypes,
  }}
/>

export const Template = (args) => (
  <StateManager initialValue={args.value}>
    {({ state, setState }) => (
      <Tabs {...args} value={state} onChange={setState}>
        <Tabs.Item key="a" value="a">
          Alpha
        </Tabs.Item>
        ,<Tabs.Item key="b" value="b">
          Bravo
        </Tabs.Item>,<Tabs.Item key="c" value="c">
          Charlie
        </Tabs.Item>,<Tabs.Item key="d" value="d" disabled>
          Delta
        </Tabs.Item>,
      </Tabs>
    )}
  </StateManager>
);

# Tabs

<Description of={Tabs} />

<Canvas withSource="none">
  <Story name="Basic" args={{ value: "a" }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Basic" />

## Sizes

Supports standard sizes — `large` `medium` `small` and custom override in pixels.

<Canvas>
  <Story name="Large">
    <StateManager initialValue="b">
      {({ state, setState }) => (
        <Tabs size="large" value={state} onChange={setState}>
          <Tabs.Item value="a">Alpha</Tabs.Item>
          <Tabs.Item value="b">Bravo</Tabs.Item>
          <Tabs.Item value="c" disabled>
            Charlie
          </Tabs.Item>
        </Tabs>
      )}
    </StateManager>
  </Story>
  <Story name="Medium">
    <StateManager initialValue={3}>
      {({ state, setState }) => (
        <Tabs size="medium" value={state} onChange={setState}>
          <Tabs.Item value={1}>One</Tabs.Item>
          <Tabs.Item value={2}>Two</Tabs.Item>
          <Tabs.Item value={3}>Three</Tabs.Item>
        </Tabs>
      )}
    </StateManager>
  </Story>
  <Story name="Small">
    <StateManager initialValue={3}>
      {({ state, setState }) => (
        <Tabs size="small" value={state} onChange={setState}>
          <Tabs.Item value={1}>One</Tabs.Item>
          <Tabs.Item value={2}>Two</Tabs.Item>
          <Tabs.Item value={3}>Three</Tabs.Item>
        </Tabs>
      )}
    </StateManager>
  </Story>
</Canvas>

## Appearance

You can modify `spacing`, `offset` and `alignment` and enable `border` to customize appearance.

<Canvas>
  <Story name="Appearance">
    <StateManager initialValue={3}>
      {({ state, setState }) => (
        <Tabs size="large" value={state} onChange={setState} border spacing={4} offset={50} alignment="left">
          <Tabs.Item value={1}>One</Tabs.Item>
          <Tabs.Item value={2}>Two</Tabs.Item>
          <Tabs.Item value={3}>Three</Tabs.Item>
        </Tabs>
      )}
    </StateManager>
  </Story>
</Canvas>

Child elements that are not of `TabItem` type are appended as custom content to the right of the tab items.

<Canvas>
  <Story name="Custom">
    <StateManager initialValue={3}>
      {({ state, setState }) => (
        <Tabs size="large" value={state} onChange={setState} border spacing={4} offset={50} alignment="left">
          <Tabs.Item value={1}>
            <Pane orientation="horizontal">
              One
              <Badge size="small" value={1} marginLeft={4} />
            </Pane>
          </Tabs.Item>
          <Tabs.Item value={2}>Two</Tabs.Item>
          <Tabs.Item value={3}>Three</Tabs.Item>
          <Pane verticalAlignment="center" marginLeft={16}>
            <IconButton iconName="i:close" size="small" />
          </Pane>
        </Tabs>
      )}
    </StateManager>
  </Story>
</Canvas>
