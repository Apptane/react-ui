import { Canvas, Meta, Story, Description, ArgsTable } from "@storybook/addon-docs";
import { PieChart } from "@apptane/react-ui";
import { PieChartData1, PieChartData2 } from "./data";

<Meta
  title="Components/Visualization/Chart — Pie"
  component={PieChart}
  argTypes={{
    data: {
      table: {
        category: "Data",
      },
    },
    totalValue: {
      table: {
        category: "Data",
      },
      control: "text",
    },
    totalLabel: {
      table: {
        category: "Data",
      },
      control: "text",
    },
    emptyText: {
      table: {
        category: "Data",
      },
      control: "text",
    },
    size: {
      table: {
        category: "Dimensions",
      },
    },
    width: {
      table: {
        category: "Dimensions",
      },
    },
    colorMode: {
      table: {
        category: "Appearance",
      },
    },
    colorScheme: {
      table: {
        category: "Appearance",
      },
    },
    color: {
      table: {
        category: "Appearance",
      },
    },
    formatValue: {
      table: {
        category: "Appearance",
      },
    },
    tooltipVisible: {
      table: {
        category: "Appearance",
      },
    },
    legendVisible: {
      table: {
        category: "Appearance",
      },
    },
    onClick: {
      table: {
        category: "Events",
      },
    },
  }}
  parameters={{
    actions: { argTypesRegex: "^on.*" },
  }}
/>

export const Template = (args) => <PieChart {...args} data={PieChartData1} />;

# Pie Chart

<Description of={PieChart} />

<Canvas withSource="none">
  <Story name="Example" args={{ size: 160 }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Example" />

## Options

<Canvas withSource="none">
  <Story name="Legend" args={{}}>
    <PieChart size={200} width={800} totalLabel="Servers" data={PieChartData2} legendVisible />
  </Story>
</Canvas>

<Canvas withSource="none">
  <Story
    name="Legend, custom size, total"
    args={{
      width: 400,
      size: 160,
      totalValue: "3000",
      totalLabel: "Guest OS",
      legendVisible: true,
      data: PieChartData1,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas withSource="none">
  <Story name="No data" args={{}}>
    <PieChart size={160} emptyText="No data" data={[]} />
  </Story>
</Canvas>
