import { Canvas, Meta, Story, Description, ArgsTable } from "@storybook/addon-docs";
import { MarginArgTypes, SelectorGroupArgTypes, StateManager } from "@apptane/react-ui-storybook-docs";
import { Button, RadioButton, ToggleGroup } from "@apptane/react-ui";

<Meta
  title="Components/Controls/ToggleGroup"
  component={ToggleGroup}
  argTypes={{
    colorMode: {
      table: {
        category: "Appearance",
      },
    },
    appearance: {
      table: {
        category: "Appearance",
      },
    },
    size: {
      table: {
        category: "Dimensions",
      },
    },
    width: {
      table: {
        category: "Dimensions",
      },
    },
    spacing: {
      table: {
        category: "Dimensions",
      },
      control: {
        type: "range",
        min: 0,
        max: 48,
        step: 1,
      },
    },
    orientation: {
      table: {
        category: "Layout",
      },
    },
    ...SelectorGroupArgTypes,
    ...MarginArgTypes,
  }}
/>

export const Template = (args) => (
  <StateManager initialValue={args.value}>
    {({ state, setState }) => (
      <ToggleGroup {...args} value={state} onChange={setState}>
        <Button key="a" value="a">
          Alpha
        </Button>
        <Button key="b" value="b">
          Bravo
        </Button>
        <Button key="c" value="c">
          Charlie
        </Button>
        <Button key="d" value="d">
          Delta
        </Button>
      </ToggleGroup>
    )}
  </StateManager>
);

# Toggle Group

<Description of={ToggleGroup} />

<Canvas withSource="none">
  <Story name="Basic" args={{ value: "a" }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Basic" />

## Buttons

Horizontal group of buttons, different sizes.

<Canvas>
  <Story name="Buttons — Text Value">
    <StateManager initialValue="b">
      {({ state, setState }) => (
        <ToggleGroup size="medium" value={state} onChange={setState}>
          <Button value="a">Alpha</Button>
          <Button value="b">Bravo</Button>
          <Button value="c" disabled>
            Charlie
          </Button>
        </ToggleGroup>
      )}
    </StateManager>
  </Story>
  <Story name="Buttons — Numeric Value">
    <StateManager initialValue={3}>
      {({ state, setState }) => (
        <ToggleGroup size="large" value={state} onChange={setState}>
          <Button value={1}>One</Button>
          <Button value={2}>Two</Button>
          <Button value={3}>Three</Button>
        </ToggleGroup>
      )}
    </StateManager>
  </Story>
</Canvas>

Vertical group of buttons of the fixed width.

<Canvas>
  <Story name="Buttons — Vertical">
    <StateManager initialValue="b">
      {({ state, setState }) => (
        <ToggleGroup size="medium" value={state} onChange={setState} orientation="vertical" width={100}>
          <Button value="a">Alpha</Button>
          <Button value="b">Bravo</Button>
          <Button value="c" disabled>
            Charlie
          </Button>
        </ToggleGroup>
      )}
    </StateManager>
  </Story>
</Canvas>

## Radio Buttons

Horizontal group of radio buttons, different sizes.

<Canvas>
  <Story name="Radio — Text Value">
    <StateManager initialValue="b">
      {({ state, setState }) => (
        <ToggleGroup size="medium" value={state} onChange={setState}>
          <RadioButton value="a" label="Alpha" />
          <RadioButton value="b" label="Bravo" />
          <RadioButton value="c" label="Charlie" disabled />
        </ToggleGroup>
      )}
    </StateManager>
  </Story>
  <Story name="Radio — Numeric Value">
    <StateManager initialValue={3}>
      {({ state, setState }) => (
        <ToggleGroup size="small" value={state} onChange={setState}>
          <RadioButton value={1} label="One" />
          <RadioButton value={2} label="Two" />
          <RadioButton value={3} label="Three" />
        </ToggleGroup>
      )}
    </StateManager>
  </Story>
</Canvas>

Vertical group of radio buttons.

<Canvas>
  <Story name="Radio — Vertical">
    <StateManager initialValue="b">
      {({ state, setState }) => (
        <ToggleGroup size="medium" value={state} onChange={setState} orientation="vertical">
          <RadioButton value="a" label="Alpha" />
          <RadioButton value="b" label="Bravo" />
          <RadioButton value="c" label="Charlie" disabled />
        </ToggleGroup>
      )}
    </StateManager>
  </Story>
</Canvas>
