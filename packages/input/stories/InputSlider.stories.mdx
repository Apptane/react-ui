import { Canvas, Meta, Story, Description, ArgsTable } from "@storybook/addon-docs";
import { InputNumericArgTypes, StateManager } from "@apptane/react-ui-storybook-docs";
import { InputSlider } from "@apptane/react-ui";

<Meta
  title="Components/Controls/Input — Slider"
  component={InputSlider}
  argTypes={{
    min: {
      table: {
        category: "Data",
      },
    },
    max: {
      table: {
        category: "Data",
      },
    },
    inputVisible: {
      table: {
        category: "Appearance",
      },
    },
    inputWidth: {
      table: {
        category: "Appearance",
      },
    },
    ...InputNumericArgTypes,
  }}
  parameters={{
    actions: { argTypesRegex: "^on.*" },
  }}
/>

export const Template = (args) => (
  <StateManager initialValue={args.value}>
    {({ state, setState }) => <InputSlider margin={48} {...args} value={state} onChange={setState} />}
  </StateManager>
);

# Input — Slider

<Description of={InputSlider} />

<Canvas withSource="none">
  <Story name="Basic" args={{ value: 0, max: 100 }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Basic" />

## Appearance

Set `min` and `max` property to control the range.

<Canvas withSource="none">
  <Story name="Custom range" args={{ value: 100, min: 100, max: 1000 }}>
    {Template.bind({})}
  </Story>
</Canvas>

Set `inputVisible` and `inputWidth` to control the visibility of the manual input field.

<Canvas withSource="none">
  <Story
    name="Manual input"
    args={{
      value: 100,
      min: 100,
      max: 1000,
      inputVisible: true,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
