import { Canvas, Meta, Story, Description, ArgsTable } from "@storybook/addon-docs";
import { MarginArgTypes, CheckBoxArgTypes, StateManager } from "@apptane/react-ui-storybook-docs";
import { InputCheck, Pane } from "@apptane/react-ui";

<Meta
  title="Components/Controls/Input — CheckBox"
  component={InputCheck}
  argTypes={{
    label: {
      table: {
        category: "Content",
      },
      control: "text",
    },
    children: {
      table: {
        category: "Content",
      },
    },
    labelWidth: {
      table: {
        category: "Content",
      },
    },
    colorMode: {
      table: {
        category: "Appearance",
      },
    },
    appearance: {
      table: {
        disable: true,
      },
    },
    indeterminateAllowed: {
      table: {
        category: "Behavior",
      },
    },
    width: {
      table: {
        category: "Dimensions",
      },
    },
    size: {
      table: {
        category: "Dimensions",
      },
    },
    inline: {
      table: {
        category: "Dimensions",
      },
    },
    ...CheckBoxArgTypes,
    ...MarginArgTypes,
  }}
/>

export const Template = (args) => (
  <StateManager initialValue={args.value}>
    {({ state, setState }) => <InputCheck {...args} value={state} onChange={setState} />}
  </StateManager>
);

# Input — CheckBox

<Description of={InputCheck} />

<Canvas withSource="none">
  <Story name="Basic" args={{ label: "Label", value: false }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Basic" />

## Sizes and States

Supports standard sizes — `large` `medium` `small`.

<Canvas withSource="none">
  <Story name="Large" args={{ label: "Large", size: "large", value: false }}>
    {Template.bind({})}
  </Story>
  <Story name="Medium" args={{ label: "Medium", size: "medium", value: false }}>
    {Template.bind({})}
  </Story>
  <Story name="Small" args={{ label: "Small", size: "small", value: false }}>
    {Template.bind({})}
  </Story>
</Canvas>

Custom `size`.

<Canvas withSource="none">
  <Story name="Custom Size">
    <StateManager initialValue={false}>
      {({ state, setState }) => (
        <InputCheck value={state} onChange={setState} size={40} label={state ? "40px — On" : "40px — Off"} />
      )}
    </StateManager>
  </Story>
</Canvas>

Set `labelWidth` to restrict label content and force ellipsis on overflow.

> Use `width` to control the maximum width of the entire component!

<Canvas>
  <Story
    name="Label width"
    args={{
      label: "Label is restricted to 200px, but is longer",
      labelWidth: 200,
      value: true,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

### Visual States

<Canvas withSource="none">
  <Story name="Visual States">
    <InputCheck marginBottom={8} label="Unchecked" />
    <InputCheck marginBottom={8} label="Checked" value={true} />
    <InputCheck marginBottom={8} label="Indeterminate" indeterminateAllowed value={undefined} />
    <InputCheck marginBottom={8} label="Checked and Disabled" value={true} disabled />
    <InputCheck marginBottom={8} label="Readonly" value={true} readonly />
    <InputCheck disabled label="Disabled" />
  </Story>
</Canvas>

### Transition

<Canvas withSource="none">
  <Story name="Transition">
    <StateManager initialValue={false}>
      {({ state, setState }) => (
        <InputCheck
          indeterminateAllowed
          value={state}
          onChange={setState}
          label={state == null ? "Indeterminate" : state ? "On" : "Off"}
        />
      )}
    </StateManager>
  </Story>
</Canvas>
