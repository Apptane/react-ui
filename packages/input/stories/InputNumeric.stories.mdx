import { Canvas, Meta, Story, Description, ArgsTable } from "@storybook/addon-docs";
import { InputNumericArgTypes, StateManager } from "@apptane/react-ui-storybook-docs";
import { InputNumeric, Tag } from "@apptane/react-ui";

<Meta title="Components/Controls/Input — Numeric" component={InputNumeric} argTypes={InputNumericArgTypes} />

export const Template = (args) => (
  <StateManager initialValue={args.value}>
    {({ state, setState }) => <InputNumeric {...args} value={state} onChange={setState} />}
  </StateManager>
);

# Input — Numeric

<Description of={InputNumeric} />

<Canvas withSource="none">
  <Story name="Basic" value={undefined}>
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Basic" />

## Sizes

Supports standard sizes — `large` `medium` `small`.

<Canvas withSource="none">
  <Story
    name="Large"
    args={{
      label: "Large",
      size: "large",
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Medium"
    args={{
      label: "Medium",
      size: "medium",
    }}>
    {Template.bind({})}
  </Story>
  <Story
    name="Small"
    args={{
      label: "Small",
      size: "small",
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Formats

Set `precision` property to the number of decimal digits accepted.

<Canvas withSource="none">
  <Story
    name="Precision"
    args={{
      label: "Numeric",
      size: "large",
      precision: 5,
      embedRight: <Tag appearance="secondary">#.00000</Tag>,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

Set `digits` to specify total number of digits accepts and `negative` for negative numbers.

<Canvas withSource="none">
  <Story
    name="Digits"
    args={{
      label: "Numeric",
      size: "large",
      digits: 7,
      precision: 2,
      negative: true,
      embedRight: <Tag appearance="secondary">±#####.##</Tag>,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

Set `prefix` and `suffix` for non-numeric content.

<Canvas withSource="none">
  <Story
    name="Prefix and suffix"
    args={{
      label: "Numeric",
      size: "large",
      prefix: "Size ",
      suffix: " KiB",
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## States

<Canvas withSource="none">
  <Story
    name="Disabled"
    args={{
      label: "Large",
      size: "large",
      disabled: true,
      value: 9999999,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas withSource="none">
  <Story
    name="Error with glyph"
    args={{
      label: "Large",
      size: "large",
      error: "Please correct the value entered",
      errorAppearance: "glyph",
      value: 123567890,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas withSource="none">
  <Story
    name="Error with hint"
    args={{
      label: "Large",
      size: "large",
      error: "Please correct the value entered",
      errorAppearance: "hint",
      value: 123567890,
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
