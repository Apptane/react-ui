import { Canvas, Meta, Story, Description, ArgsTable } from "@storybook/addon-docs";
import { Fragment } from "react";
import { MarginArgTypes, PaddingArgTypes } from "@apptane/react-ui-storybook-docs";
import { Card, Text, TileLayout } from "@apptane/react-ui";

<Meta
  title="Components/Layout/Tile"
  component={TileLayout}
  argTypes={{
    width: {
      table: {
        category: "Dimensions",
      },
    },
    height: {
      table: {
        category: "Dimensions",
      },
    },
    shadow: {
      table: {
        category: "Appearance",
      },
    },
    tileGap: {
      table: {
        category: "Layout",
      },
      control: {
        type: "range",
        min: 0,
        max: 64,
        step: 1,
      },
    },
    minTileWidth: {
      table: {
        category: "Layout",
      },
    },
    ...MarginArgTypes,
    ...PaddingArgTypes,
  }}
/>

export const Content = ({ quantity = 30 }) => (
  <Fragment>
    {[...Array(quantity).keys()].map((index) => (
      <Card
        key={`card-${index}`}
        background="light"
        interactivity="border"
        focusable
        inline
        height={150}
        verticalAlignment="center"
        horizontalAlignment="center">
        <Text>{`card-${index}`}</Text>
      </Card>
    ))}
  </Fragment>
);

export const Template = (args) => (
  <TileLayout {...args}>
    <Content />
  </TileLayout>
);

# Tile Layout

<Description of={TileLayout} />

<Canvas>
  <Story name="Example" args={{ height: 400, margin: -6, padding: 6, tileGap: 32, minTileWidth: 200, shadow: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Example" />
